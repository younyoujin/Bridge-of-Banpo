1. 파이썬은 수학을 할 줄 안다.
2. 문자열은 큰 따옴표나, 작은 따옴표로 감싸야 한다.
3. 문자열 끼리 + 로 연결 할 수 있다.
4. 문자열에 곱 연산을 할 수 있다.
5. 문자열에 작은 따옴표를 넣고 싶은경우 \기호를 앞에 붙이거나 큰 따옴표로 싸여진 문자열 안에서 가능
6. 문자열.upper()로 대문자로 전환이 가능
7. len("문자열") 을 사용하면 글자의 수를 알 수 있다.
8. str함수는 대상을 문자열 로 변환한다.
   int 함수는 대상을 정수 로 변환한다.
9. 변수 선언은 a = b 형식으로 한다.
10. print() 함수를 사용할 경우 따옴표가 사라진다.
11. 리스트 : 다른 객체들을 나열한 객체 []
12. len(리스트) 를 할 경우 객체의 수를 알려준다.
13. 리스트.sort() 할 경우 오름차순으로 정렬한다.
14. 리스트.reverse() 할 경우 내림차순으로 정렬한다.
15. 리스트.append() 할 경우 맨 뒤에 객체를 대입한다.
16. 리스트.pop(index) 할 경우 해당 index의 값을 삭제한다.
17. 딕셔너리 : 리스트와 유사하지만, 인덱스가 아닌 키로 값을 찾는다. {}
18. 들여쓰기는 4칸
19. 조건문 뒤에는 : 콜론 기호를 사용
20. 함수 정의 def
21. for 파라미터 in 리스트, 딕셔너리
22. for i in range(1,6) 1~5까지가 출력된다. range는 반만 열려있어서 마지막 숫자는 포함하지 않는다.
